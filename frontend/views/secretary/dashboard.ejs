<%- include('../partials/header', { user: user }) %>

    <div class="main-content">
        <div class="page-header">
            <h2>
                <%= t.secretary_dash_title %>
            </h2>
        </div>

        <div class="dashboard-grid">
            <div class="glass-card dash-card">
                <h3>👥 <%= t.secretary_dash_students %>
                </h3>
                <p>
                    <%= t.secretary_dash_students_desc %>
                </p>
                <a href="/students" class="btn-primary">
                    <%= t.nav_students %>
                </a>
            </div>
            <div class="glass-card dash-card">
                <h3>💰 <%= t.secretary_dash_payments %>
                </h3>
                <p>
                    <%= t.secretary_dash_payments_desc %>
                </p>
                <a href="/students" class="btn-primary">
                    <%= t.nav_students %>
                </a>
            </div>
        </div>
    </div>

    <style>
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .dash-card {
            display: flex;
            flex-direction: column;
            border-top: 4px solid transparent;
            transition: border-color 0.2s;
        }

        .dash-card:hover {
            border-top-color: var(--primary-color);
        }

        .dash-card h3 {
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }

        .dash-card p {
            color: var(--text-muted);
            font-size: 0.95rem;
            flex-grow: 1;
            margin-bottom: 1.5rem;
        }
    </style>

    <%- include('../partials/footer') %>